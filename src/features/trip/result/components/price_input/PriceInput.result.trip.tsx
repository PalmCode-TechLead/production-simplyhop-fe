import * as React from "react";
import clsx from "clsx";

export interface PriceInputResultTripProps {}

export const PriceInputResultTrip = (props: PriceInputResultTripProps) => {
  return (
    <div className={clsx("")}>
      <div></div>
    </div>
  );
};
