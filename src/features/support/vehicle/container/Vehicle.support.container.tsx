import * as React from "react";
import clsx from "clsx";

export const VehicleSupportContainer = () => {
  return (
    <div className={clsx("W-full h-full", "relative")}>
      <div></div>
    </div>
  );
};
