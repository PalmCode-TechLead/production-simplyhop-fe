import {
  Search,
  CarFront,
  Navigation,
  MessageSquare,
  User,
} from "lucide-react";

export const svgMap = {
  Search,
  CarFront,
  Navigation,
  MessageSquare,
  User,
};

export type SVGName = keyof typeof svgMap;
