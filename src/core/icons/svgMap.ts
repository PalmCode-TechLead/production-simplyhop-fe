import {
  Search,
  CarFront,
  Navigation,
  MessageSquare,
  User,
  Check,
} from "lucide-react";

export const svgMap = {
  Search,
  CarFront,
  Navigation,
  MessageSquare,
  User,
  Check,
};

export type SVGName = keyof typeof svgMap;
