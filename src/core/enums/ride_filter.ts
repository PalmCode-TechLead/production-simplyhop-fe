export enum RIDE_FILTER {
  ORIGIN = "origin_id",
  DESTINATION = "destination_id",
  DATE = "date",
  ADULT_PASSENGER = "adult",
  CHILDREN_PASSENGER = "children",
  CAR_SEAT = "car_seat",
  RIDE_ID = "ride_id",
}
